/** Canais de venda validos (RF-05). Unica fonte de verdade — antes duplicado
 * em catalogo/routes.js e pedidos/routes.js. */
const CANAIS = ['APP', 'TOTEM', 'BALCAO', 'PICKUP'];

module.exports = { CANAIS };
